<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="450"
             x:Class="Todo.Views.TodoListView">

    <DockPanel>
        <Button DockPanel.Dock="Bottom" Content="Add an item" Command="{Binding $parent[Window].DataContext.AddItem}"/>
        <Button DockPanel.Dock="Bottom" Content="Remove selected" Command="{Binding Remove}"/>
        <ItemsControl Items="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="3" Padding="2" BorderThickness="1" BorderBrush="Black">
                        <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding Description}" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
