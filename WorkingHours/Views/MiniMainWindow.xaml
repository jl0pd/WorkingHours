<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WorkingHours.ViewModels;assembly=WorkingHours"
        mc:Ignorable="d" d:DesignWidth="250" d:DesignHeight="50"
        x:Class="WorkingHours.Views.MiniMainWindow"
        SizeToContent="WidthAndHeight"
        Topmost="True" HasSystemDecorations="False">
  <Border BorderThickness="1" BorderBrush="{DynamicResource ThemeAccentBrush}">
    <DockPanel x:Name="DockPanel">
      <Button x:Name="CloseButton" MaxWidth="32" Padding="0" Click="OnCloseClick">
        <Image Source="avares://WorkingHours/Assets/icons8-cancel-32.png" />
      </Button>
    </DockPanel>
  </Border>
</Window>
